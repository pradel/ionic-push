{
  "name": "node-ionic-push",
  "version": "0.0.1",
  "description": "Ionic push server for nodejs",
  "main": "lib/main.js",
  "scripts": {
    "build": "babel src --out-dir lib",
    "lint": "eslint src",
    "prepublish": "npm run build",
    "test": "npm run lint"
  },
  "dependencies": {
    "bluebird": "^3.3.4",
    "request": "^2.69.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.7.2",
    "babel-preset-es2015": "^6.6.0",
    "eslint": "^2.4.0",
    "eslint-config-airbnb": "^6.1.0"
  }
}
